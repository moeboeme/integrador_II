#include<stdio.h>

void punto_3_divisas() ;

int main()
{
    punto_3_divisas() ;
    return 0 ;
}

/*
3) Una corporación internacional se dedica a la compra/venta de divisas. Dicha empresa
opera con tres monedas de cambio: pesos, dólares y euros.
Se solicita el desarrollo de un programa que, partiendo de un capital inicial EN PESOS,
permita hacer una simulación de hasta 20 transacciones de compra/venta.
Para cada transacción, se ingresará el código de operación (C/V), el código de divisa
(P/D/E) y el monto de la operación. Se deberán validar los códigos ingresados, y además
que el monto de la operación sea mayor a cero.
El monto de la transacción se restará o sumará al capital inicial, de acuerdo a la
operación (una compra suma al capital inicial, una venta resta al capital inicial) y
la relación de cambio (como constantes), y pesificando el resultado cuando sea
necesario.
La carga finaliza cuando se ingresa un código de operación 'S' (salir).
Entonces, el programa mostrará:

* cuántas transacciones se realizaron en total según la operación,
* cuál fue la operación de MAYOR valor de VENTA,
* cuál fue la operación de MENOR valor de COMPRA,
* cuál fue el saldo al cierre del mercado.
* si hubo ganancia, cuál fue el monto resultante.
*/

 void punto_3_divisas()
 {

 }
